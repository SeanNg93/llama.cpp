# llama.cpp daily summary — 2026-04-27

Today's activity focused on GPU backend performance work across CUDA, OpenCL, and WebGPU, alongside CPU quantization improvements and a notable bug fix for recurrent model inference. Eight PRs were merged in the past 24 hours.

## OpenCL

- **Add IQ4_NL quantization support** — general implementation plus Adreno-specific GEMM/GEMV kernels; packs 2 LUT entries into a uint for efficiency. Large addition (~1 700 lines). (#22272, `f454bd7`)

## CUDA / GPU

- **Better coalesced data-access for contiguous concat** — addresses uncoalesced stores found via Nsight and distributes elements evenly across CTAs; yields 1–3% E2E speedup on Nemotron-H and Qwen MoE models during token generation. (#22330, `b1a5bd4`)

## WebGPU

- **Performance-portable matmul tuning knobs** — splits `WEBGPU_MUL_MAT_TILE_K` into separate register-tile and subgroup-matrix constants; new defaults raise average achieved GFLOPs from ~59% to ~80% of per-GPU optimum across four tested GPUs (RTX 5080, RX 7900 XT, Arc B580, Apple M2). (#22241, `06a811d`)

## CPU

- **Optimize AVX2 Q6_K dequantization** — ports earlier AVX optimizations to AVX2, yielding ~27% improvement in `pp512` throughput for Q6_K on tested hardware. (#22345, `2dd8416`)
- **Re-enable fast `gelu_quick_f16`** — simplifies the vec.h implementation and restores a previously-disabled fast path. (#22339, `0c6ee1c`)

## Bug fixes

- **Fix recurrent state serialization for partial tensor reads/writes** — previous code only handled full-tensor I/O and triggered `GGML_ASSERT(size == ggml_nbytes(tensor))` in llama-server when using tensor-parallel recurrent models (e.g. Mamba/SSM). (#22362, `78433f6`)

## Infra / meta

- **CODEOWNERS: assign meta backend owner** (Johannes Gäßler). (#22388, `7ec36aa`)
- **pr2wt script: symlink `.pi/SYSTEM.md`** into worktrees. (#22386, `f535774`)

---

## AI Policy Closure Report

Of the 4 PRs closed without merging in this period, an estimated **1–2** appear to have been closed for AI policy violations:

- **#22164** ("common: improve GGUF quantization tag regex") — body is structured in a characteristic AI-generated style (`## Summary` / `## Motivation` sections), no AI disclosure was filled in, and the PR was closed with no review comments. This is the strongest candidate for an AI-policy closure.
- **#22387** ("Fix broken links") — minimal body, closed quickly with a single comment; may also have been flagged.

The remaining two closed PRs (#22380, #21247) appear to have been closed for other reasons (incomplete submission and ongoing review discussions, respectively).

> Note: Comment text for upstream PRs was not directly accessible; this assessment is based on PR metadata and body content patterns.
