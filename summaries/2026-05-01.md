# llama.cpp daily summary — 2026-05-01

A moderately active day with a strong focus on WebGPU performance, backend API additions, and speculative-decoding reliability. The most substantial changes land in ggml-webgpu, where both matrix-vector and matrix-matrix paths for MUL_MAT_ID were significantly overhauled. No breaking changes detected; the Vulkan tensor 2D API addition touches many backends but follows the existing interface pattern.

## WebGPU

- **MUL_MAT_ID mat-vec + mat-mat overhaul** — new fast paths and i-quant support for mixture-of-experts dispatch, with a shared accumulation template refactor that removes ~1300 lines of generated WGSL (#22464, `a95a11e`)
- **Fast i-quant matmul declarations** — adds `mul_mat_decls.tmpl` for accelerated iquant mat-mat in WebGPU (#22504, `4515559`)

## CUDA / GPU

- **Fix tiled FlashAttention kernel on Pascal (SM60)** — corrects a register/shared-memory issue that caused incorrect results on older NVIDIA cards (#22541, `e82aaf2`)

## Vulkan

- **Add `get/set_tensor_2d` functions** — implements the 2D tensor transfer interface in the Vulkan backend, with stub updates across all other backends (#22514, `660b1b4`)

## Speculative decoding

- **Fix draft model checkpoint handling** — corrects checkpoint logic in `speculative.cpp`; verbose-gates the ngram-mod reset warning; related server-context cleanup (#22521, `80afa33`)
- **Fix vocab compatibility check in speculative example** — uses `vocab_tgt`/`vocab_dft` instead of context pointers when logging vocabulary mismatches (#22426, `b42c7fa`)

## Build / vendor

- **llama-mmap: use `ftello`/`fseeko`** — fixes 32-bit Wasm builds and enables support for models >2 GB on 32-bit targets (#22497, `5cbfb18`)
- **Update cpp-httplib to 0.43.2** (#22548, `5f0ab72`)

## Fixes / misc

- **Null-check `getpwuid` in hf-cache** — prevents a crash when the user entry is missing (#22550, `beb42ff`)
- **Fix argument typo in spec CLI** (#22552, `c20c445`)
- **CI: bump `ty` linter to 0.0.33** with updated typings (#22535, `6118c04`)
- **Add `wc2wt.sh` script** — creates a git worktree from the current HEAD for local development branches (#22513, `27aef3d`)

---

## AI Policy Enforcement Report

**PRs closed for improper AI policy usage (last 24 h): 1**

- **#22559** — *llama-quant : honor --tensor-type override when it matches the global ftype* — closed without merge by a maintainer. The PR description was explicitly generated by Claude Code ("🤖 Generated with Claude Code"), violating the project's policy against AI-written PR descriptions.
