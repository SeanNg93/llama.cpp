# llama.cpp daily summary — 2026-04-23

A busy day dominated by WebGPU work and GPU backend improvements. Twelve commits landed, with notable new model support for HunyuanVL, significant WebGPU feature additions, and a server/chat refactor that consolidates conversion logic.

## WebGPU

- Add conv2d shader kernels (f32 and f16 passes tests), plus f16 stability fixes for unary ops and GET_ROWS (#21964, `ca7f7b7`)
- Add fused RMS_NORM + MUL kernel for improved throughput (#21983, `6da7168`)
- Implement async tensor API (`set_tensor_async`, `synchronize`) and GPU event API (`create`/`delete`) using direct `queue.WriteBuffer` (#22099, `0d0764d`)

## HIP / AMD GPU

- **`GGML_HIP_GRAPHS` is now on by default** — benchmarks show 3–14% speedup on gfx1100 and gfx908; previously disabled due to earlier ROCm regressions (#22254, `6217b49`)

## SYCL

- Fix `mul_mat_id` OOM on Level Zero (MoE models with `--cpu-moe`) by sizing staging buffers to actual routed rows; add BF16×BF16→F32 DNNL fast path for large-vocab lm_head to avoid multi-GB f32 allocs (#22119, `225088e`)

## Multimodal (mtmd)

- **HunyuanVL vision-language model support**: new `LLM_ARCH_HUNYUAN_VL` with M-RoPE (XD-RoPE), `PROJECTOR_TYPE_HUNYUANVL` with PatchMerger encoder, GGUF conversion support (#22037, `7bfe60f`)
- Support `LLAMA_ROPE_TYPE_NONE` in mtmd (#22242, `82d3f4d`)

## API / Server

- Refactor `common/chat` and server: move all OpenAI↔llama conversion functions to `common`, split into new `server-chat.cpp`, add tests (#20690, `134d6e5`)
- Fix `parallel_tool_calls` default based on model capabilities; add test scripts for parallel tool calls and structured outputs (#22217, `8bccdbb`)
- Ignore reasoning content from transcription API responses (#21905, `17f6245`)

## Speculative decoding

- Add checkpoint support to `speculative-simple` example to allow resuming long inference runs (#22227, `bcb5eeb`)

## Misc / Refactor

- Move `reasoning_budget_message` from common params into sampling params; remove redundant `reasoning_budget` common param (#22233, `750579f`)

---

## AI Policy Enforcement

**PRs closed for improper AI usage in the last 24 hours: 1**

- **#22225** ("build: install shared library DLLs on Windows") — closed without merging. Contributor explicitly disclosed "AI (Opus 4.6) was used to diagnose the root cause and **draft the fix and PR description**." Writing PR descriptions with AI is prohibited under AGENTS.md.
