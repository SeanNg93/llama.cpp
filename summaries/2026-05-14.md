# llama.cpp daily summary — 2026-05-14

A busy day with 16 commits spanning GPU backend improvements, server/WebUI enhancements, and CI hardening. The biggest additions are expanded OpenCL MoE quantization support for Adreno GPUs and two notable server features enabling vLLM API compatibility and continued generation on reasoning models.

## OpenCL / Adreno
- **Add q5_0 and q5_1 MoE quantization support** for Adreno GPUs, including new GEMM/GEMV kernels (#22985, `ec562eb`) — large addition (~1,900 lines)
- **Fix crash when warming up MoE** on Adreno during model initialization (#22876, `1e4579f`)

## WebGPU
- **Guard subgroup-matrix path** to only activate when head dimensions are divisible by `sg_mat_k` / `sg_mat_n`, fixing correctness issues (#23020, `4c1c3ac`)
- **Flush GPU profile timestamp** before the query set overflows, preventing profiling data loss (#22995, `527045b`)

## CUDA
- **Fix half+half operator ambiguity** in allreduce by casting intermediate results to float first; resolves issue #22974 (#22994, `7f3f843`)

## API / Server / WebUI
- **vLLM API compat**: accept `continue_final_message` flag — when set with `add_generation_prompt: false`, triggers the existing prefill path; mutual exclusion with `add_generation_prompt: true` enforced with HTTP 400 (#23012, `95d469a`)
- **Continue generation on reasoning models**: orchestrate thinking tags around prefilled messages so the stream parser routes chunks correctly; WebUI persists partial CoT on stop (#22727, `5d44db6`)
- **Deduplicate model aliases** in server data and improve alias display in WebUI (#22979, `2dfeca3`)
- **Preserve system message on edit cancel** in WebUI (#22911, `46be24d`)

## Hexagon (Qualcomm HTP)
- **Add unary tanh op** to the Hexagon backend (#22999, `ad96bb8`)

## ZenDNN
- **Adaptive fallback to CPU** for small batch sizes, controlled via `GGML_ZENDNN_ADAPTIVE_FALLBACK` env var (default: enabled) (#22681, `61af07c`)

## Speculative / CLI
- **Update CLI argument names** for speculative decoding for better consistency across tools (#22964, `634275f`)

## Build / CI
- **Validate model naming convention** in CI via a new GitHub Actions workflow (#22680, `3796c94`)
- **Enable type checking** in `llama-eval.py` (#22988, `bcfe63f`)

## Fixes / Misc
- **download**: replace `exit()` with proper error return to avoid hard crashes on download failure (#23008, `e75cd5e`)
- **docs**: update OPENVINO.md with validated models and quantizations (#22959, `7e16646`)

---

**PRs closed due to improper AI policy usage:** 0
